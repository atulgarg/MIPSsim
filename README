MIPS simulator using Tomasulo algorithm and Pipeline components using Branch target buffer(1-bit predictor), Reorder buffer, 32-bit architecture and double arithmatic operations.
TO DO
1. make architecture, entries in BTB,ROB configurable.
2. use getopt instead of dirty parsing.
